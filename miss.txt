

---

변수: `{pid}`, `{blocker_pid}`

---

## A. 림 근처 (Restricted_Area) — 존 언급 없음

### SHOT_RIM_DUNK (2점)

**일반 MISS**
`{pid}`의 덩크 시도! 림을 때리고 튕겨 나옵니다!

**인플레이 BLOCK**
`{pid}` 덩크로 올라갑니다! `{blocker_pid}`가 위에서 쳐내며 블록! 공은 살아있습니다!

**데드볼 BLOCK(OOB)**
`{pid}` 덩크 시도! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼입니다!

---

### SHOT_RIM_LAYUP (2점)

**일반 MISS**
`{pid}`의 레이업! 살짝 빗나갑니다!

**인플레이 BLOCK**
`{pid}` 레이업으로 마무리! `{blocker_pid}`가 손끝으로 쳐내며 블록! 인플레이!

**데드볼 BLOCK(OOB)**
`{pid}` 레이업 시도! `{blocker_pid}`가 막아냈고 공이 밖으로! 데드볼!

---

### SHOT_RIM_CONTACT (2점)

**일반 MISS**
`{pid}` 접촉을 버티고 올렸지만 마무리가 안 됩니다!

**인플레이 BLOCK**
`{pid}` 몸싸움 속 슛! `{blocker_pid}`가 끝까지 따라와 블록! 공은 인플레이!

**데드볼 BLOCK(OOB)**
`{pid}` 컨택 속 마무리! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼입니다!

---

## B. 페인트 비-RA (Paint_Non_RA) — 존 언급 없음

### SHOT_TOUCH_FLOATER (2점)

**일반 MISS**
`{pid}`의 플로터! 터치가 조금 길었습니다!

**인플레이 BLOCK**
`{pid}` 플로터로 띄웁니다! `{blocker_pid}`가 손끝으로 쳐내요! 공은 살아있어요!

**데드볼 BLOCK(OOB)**
`{pid}` 플로터 시도! `{blocker_pid}`가 쳐냈고 공이 밖으로! 데드볼!

---

### SHOT_POST (2점)

**일반 MISS**
`{pid}` 포스트에서 돌아서 마무리! 림이 허락하지 않네요!

**인플레이 BLOCK**
`{pid}` 포스트 슛으로 마무리! `{blocker_pid}`가 위에서 찍어 눌러 블록! 인플레이!

**데드볼 BLOCK(OOB)**
`{pid}` 포스트 훅 시도! `{blocker_pid}`가 쳐냈고 공이 밖으로! 데드볼!

---

## C. 미드레인지 (Mid_Range)

### SHOT_MID_PU (2점)

**일반 MISS**

* [존 포함] `{pid}` 미드레인지 풀업 점퍼! 림을 맞고 빠집니다!
* [존 미포함] `{pid}` 풀업 점퍼! 림을 맞고 빠집니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 미드레인지 풀업! `{blocker_pid}`가 타이밍 맞춰 쳐냈습니다! 공은 살아있습니다!
* [존 미포함] `{pid}` 풀업으로 올라갑니다! `{blocker_pid}`가 쳐냈습니다! 인플레이!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 미드레인지 풀업! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼!
* [존 미포함] `{pid}` 풀업 점퍼 시도! `{blocker_pid}`가 막아냈고 공이 밖으로! 데드볼입니다!

---

### SHOT_MID_CS (2점)

**일반 MISS**

* [존 포함] `{pid}` 미드레인지 캐치 앤 슛! 아쉽게 빗나갑니다!
* [존 미포함] `{pid}` 받아서 바로 던집니다! 아쉽게 빗나갑니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 미드레인지에서 바로 던집니다! `{blocker_pid}`가 릴리즈를 읽고 블록! 인플레이!
* [존 미포함] `{pid}` 곧바로 점퍼! `{blocker_pid}`가 읽고 쳐냈습니다! 공은 살아있어요!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 미드레인지 캐치 앤 슛! `{blocker_pid}`가 쳐냈고 공이 밖으로! 데드볼입니다!
* [존 미포함] `{pid}` 캐치 앤 슛! `{blocker_pid}`가 막아냈고 아웃 오브 바운즈! 데드볼!

---

## D. 3점 (Corner_3 / ATB_3)

### SHOT_3_CS (3점) — Corner_3

**일반 MISS**

* [존 포함] `{pid}` 코너 3점 캐치 앤 슛! 림을 외면합니다!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! 아쉽게 빗나갑니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 코너에서 캐치 앤 슛 3점! `{blocker_pid}`가 클로즈아웃으로 쳐냈습니다! 공은 살아있어요!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! `{blocker_pid}`가 쳐냈습니다! 인플레이!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 코너 3점 캐치 앤 슛! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! `{blocker_pid}`가 막아냈고 공이 밖으로! 데드볼입니다!

---

### SHOT_3_CS (3점) — ATB_3(윙/탑)

**일반 MISS**

* [존 포함] `{pid}` 윙/탑 3점 캐치 앤 슛! 아쉽게 빗나갑니다!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! 림을 외면합니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 윙/탑에서 캐치 앤 슛 3점! `{blocker_pid}`가 쳐냈습니다! 인플레이!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! `{blocker_pid}`가 쳐냈습니다! 공은 살아있습니다!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 윙/탑 캐치 앤 슛 3점! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼입니다!
* [존 미포함] `{pid}` 캐치 앤 슛 3점! `{blocker_pid}`가 막아냈고 공이 밖으로! 데드볼!

---

### SHOT_3_OD (3점) — Corner_3

**일반 MISS**

* [존 포함] `{pid}` 코너에서 드리블 3점! 짧았습니다!
* [존 미포함] `{pid}` 드리블에서 바로 3점! 짧았습니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 코너 드리블 3점으로 올라갑니다! `{blocker_pid}`가 끝까지 따라와 블록! 공은 살아있습니다!
* [존 미포함] `{pid}` 드리블 3점 시도! `{blocker_pid}`가 쳐냈습니다! 인플레이!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 코너 드리블 3점! `{blocker_pid}`가 막아냈고 공이 밖으로! 데드볼!
* [존 미포함] `{pid}` 드리블 3점! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼입니다!

---

### SHOT_3_OD (3점) — ATB_3(윙/탑)

**일반 MISS**

* [존 포함] `{pid}` 윙/탑에서 드리블 3점! 림을 맞고 나옵니다!
* [존 미포함] `{pid}` 드리블에서 바로 3점! 림을 외면합니다!

**인플레이 BLOCK**

* [존 포함] `{pid}` 윙/탑 드리블 3점! `{blocker_pid}`가 쳐냈습니다! 공은 인플레이!
* [존 미포함] `{pid}` 드리블 3점으로 올라갑니다! `{blocker_pid}`가 쳐냈습니다! 인플레이!

**데드볼 BLOCK(OOB)**

* [존 포함] `{pid}` 윙/탑 드리블 3점! `{blocker_pid}`가 쳐냈고 아웃 오브 바운즈! 데드볼입니다!
* [존 미포함] `{pid}` 드리블 3점! `{blocker_pid}`가 쳐냈고 공이 밖으로! 데드볼!

---
